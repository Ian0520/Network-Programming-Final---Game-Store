{
  "gameId": "bomb_pass_cli",
  "name": "Bomb Pass (CLI)",
  "version": "1.0.9",
  "developer": "CHANGE_ME",
  "description": "Pass the bomb before the timer expires. Last player remaining wins. CLI client.",
  "clientType": "cli",
  "minPlayers": 2,
  "maxPlayers": 3,
  "entrypoints": {
    "server": {
      "module": "server_main.py",
      "argv": ["--port", "{port}", "--token", "{token}", "--room-id", "{roomId}"]
    },
    "client": {
      "module": "client_main.py",
      "argv": ["--host", "{host}", "--port", "{port}", "--token", "{token}", "--room-id", "{roomId}", "--user", "{userId}"]
    }
  }
}
